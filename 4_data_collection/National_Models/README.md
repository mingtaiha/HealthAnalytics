This folder contains the National Level Models for each attribute. The models
are derived from the National Level Data and are in the form of a JSON tree.
Each node has the attributes: key, value, children. All nodes except the root
node contain the attributes: Min, Max (for the prediction ranges).
